/**
 * Determina si un número es par o impar.
 * @param {number} n - El número a evaluar.
 * @returns {boolean} - Devuelve true si el número es par, de lo contrario devuelve false.
 */

function esPar(n) {
    if(n % 2 === 0) {
        return true;
    } else {
        return false;
    }
}

